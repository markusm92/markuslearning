<h1>Welcome, <%= current_user.email %>!</h1>

<p>Here are the pages you can view:</p>

<ul>
  <% @pages.each do |page| %>
    <li>
      <%= link_to page.title, page_path(page.slug) %>
    </li>
  <% end %>
</ul>
